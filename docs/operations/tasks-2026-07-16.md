# Tasks de trabalho - 2026-07-16

Este arquivo registra a distribuicao das atividades feitas hoje para facilitar commits pequenos, revisao e rastreio posterior.

## Task 1 - Bootstrap executavel da stack

Escopo sugerido para commit:

- Documentar como rodar cada app localmente.
- Ajustar ERP Mock para porta `18082`.
- Alinhar BFF para consumir `http://localhost:18082`.
- Evitar falha local do BFF por `Jwt:Secret` ausente em ambiente de teste.
- Atualizar `.env.example`, READMEs e documentos de arquitetura com portas e variaveis.

Arquivos principais:

- `README.md`
- `.env.example`
- `apps/api-springboot/README.md`
- `apps/api-springboot/src/main/resources/application.properties`
- `apps/bff-dotnet/README.md`
- `apps/bff-dotnet/src/Painel.Bff/Program.cs`
- `apps/bff-dotnet/src/Painel.Bff/appsettings.json`
- `apps/bff-dotnet/src/Painel.Bff/Properties/launchSettings.json`
- `docs/`
- `render.yaml`

Validacao:

- `apps/api-springboot`: `.\mvnw.cmd test`
- `apps/bff-dotnet`: `dotnet build Painel.sln -c Release`
- `apps/bff-dotnet`: `dotnet test Painel.sln -c Release --no-restore`

## Task 2 - Refatoracao visual e organizacao Angular

Escopo sugerido para commit:

- Separar templates e estilos das paginas Angular.
- Refatorar layout principal, login, dashboard, detalhe, integracoes e configuracoes.
- Adicionar componentes compartilhados de cabecalho, chip de status e splash/loading.
- Usar `logo.svg`, `hcp-logo.svg`, `bg-login.jpg` e `favicon.ico`.
- Corrigir responsividade mobile-first e evitar textos ocultos em cards/listas.
- Corrigir `<base href="/">` para navegacao direta em rotas como `/projects/p1`.

Arquivos principais:

- `apps/web-angular/src/app/features/**`
- `apps/web-angular/src/app/layout/**`
- `apps/web-angular/src/app/shared/**`
- `apps/web-angular/src/assets/**`
- `apps/web-angular/src/index.html`
- `apps/web-angular/src/styles.scss`
- `apps/web-angular/e2e/app.spec.ts`

Validacao:

- `apps/web-angular`: `npm run lint`
- `apps/web-angular`: `npm run build`
- `apps/web-angular`: `npm test`
- `apps/web-angular`: `npm run e2e`

## Task 3 - Tela de regras e integracoes

Escopo sugerido para commit:

- Melhorar tela de regras de status para leitura rapida em telas grandes.
- Transformar tela de integracoes em area operacional com progresso, pendencias e URLs configuraveis.
- Adicionar runtime config em `assets/app-config.json`.
- Permitir editar URL da API consumida pelo frontend e URL do ERP Mock sem recompilar o Angular.

Arquivos principais:

- `apps/web-angular/src/app/features/settings/**`
- `apps/web-angular/src/app/features/integrations/**`
- `apps/web-angular/src/app/core/config/runtime-config.ts`
- `apps/web-angular/src/assets/app-config.json`
- `apps/web-angular/src/main.ts`

Validacao:

- `apps/web-angular`: `npm run lint`
- `apps/web-angular`: `npm run build`
- `apps/web-angular`: `npm run e2e`

## Task 4 - ERP Mock persistente com seed Datafaker

Escopo sugerido para commit:

- Migrar ERP Mock de dados em memoria para JPA/PostgreSQL.
- Criar seed idempotente com Datafaker.
- Gerar projetos, analistas, alocacoes e apontamentos com descricoes plausiveis.
- Manter H2 em testes para nao depender do Docker local.

Arquivos principais:

- `apps/api-springboot/pom.xml`
- `apps/api-springboot/src/main/java/br/com/painel/rm/mock/MockDataStore.java`
- `apps/api-springboot/src/main/java/br/com/painel/rm/mock/MockDataSeeder.java`
- `apps/api-springboot/src/main/java/br/com/painel/rm/persistence/**`
- `apps/api-springboot/src/main/resources/application.properties`
- `apps/api-springboot/src/test/resources/application.properties`

Validacao:

- `apps/api-springboot`: `.\mvnw.cmd test`

## Task 5 - Eventos ERP, webhook e fila local no BFF

Escopo sugerido para commit:

- Adicionar geracao de eventos no Spring nos intervalos de 3, 5, 17, 40 e 86 minutos.
- Expor `GET /rm/events` e `POST /rm/events/generate`.
- Publicar eventos no webhook do BFF.
- Receber eventos no BFF via `POST /api/v1/erp-webhooks/rm-events`.
- Persistir eventos em `WebhookInbox` como fila local.
- Adicionar endpoints de pendencias:
  - `GET /api/v1/erp-webhooks/pending-count`
  - `POST /api/v1/erp-webhooks/acknowledge`
- Adicionar sininho de alerta no Angular para dados novos disponiveis.

Arquivos principais:

- `apps/api-springboot/src/main/java/br/com/painel/rm/domain/RmEventDto.java`
- `apps/api-springboot/src/main/java/br/com/painel/rm/mock/RmEventGenerator.java`
- `apps/api-springboot/src/main/java/br/com/painel/rm/mock/RmEventScheduler.java`
- `apps/api-springboot/src/main/java/br/com/painel/rm/api/RmController.java`
- `apps/bff-dotnet/src/Painel.Bff/Controllers/ErpWebhooksController.cs`
- `apps/bff-dotnet/src/Painel.Infrastructure/Persistence/PainelDbContext.cs`
- `apps/web-angular/src/app/shared/ui/notification-bell.component.ts`
- `apps/web-angular/src/app/core/adapters/api.ts`
- `apps/web-angular/src/app/core/adapters/sync.service.ts`
- `apps/web-angular/src/app/layout/**`

Validacao:

- `apps/api-springboot`: `.\mvnw.cmd test`
- `apps/bff-dotnet`: `dotnet build Painel.sln -c Release`
- `apps/bff-dotnet`: `dotnet test Painel.sln -c Release --no-restore`
- `apps/web-angular`: `npm run lint && npm run build && npm test && npm run e2e`

## Task 6 - Documentacao operacional

Escopo sugerido para commit:

- Atualizar instrucoes de execucao local.
- Documentar variaveis de ambiente de Postgres, seed e webhook.
- Documentar runtime config do Angular.
- Manter registro das tasks para commits futuros.

Arquivos principais:

- `.env.example`
- `apps/api-springboot/README.md`
- `apps/bff-dotnet/README.md`
- `apps/web-angular/README.md`
- `docs/operations/tasks-2026-07-16.md`

Validacao:

- Revisao manual dos comandos documentados.
- Conferir consistencia de portas: Angular `4200`, BFF `5080`, ERP Mock `18082`.

## Ordem recomendada de commits

1. `docs: documenta execucao local da stack`
2. `fix: estabiliza bootstrap local do erp e bff`
3. `refactor(web): reorganiza telas e componentes angular`
4. `feat(web): adiciona identidade visual e ajustes mobile`
5. `feat(web): configura urls runtime e alerta de pendencias`
6. `feat(erp): persiste mock em postgres com seed datafaker`
7. `feat(erp): gera eventos periodicos e publica webhook`
8. `feat(bff): recebe webhook rm em fila local`
9. `docs: registra operacao de seed webhook e tasks`

## Observacoes para retomada

- Nao foi feito commit.
- Antes de commitar, revisar `git status --short` e separar os arquivos por task.
- Se houver mudancas geradas automaticamente em `target/`, `bin/`, `obj/` ou `dist/`, nao incluir.
- Os comandos de validacao ja passaram ao final do ciclo de hoje.
